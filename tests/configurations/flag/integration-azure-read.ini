[default]
provider        = azure
account_url     = https://dscccccccccccccc.blob.core.windows.net
auth_type       = passwordless
container_name  = flag-read

# This flag does nothing with the 'r' mode.
# But in tests we write data into the container before reading it, and so the 'r' mode is used after the 'c' mode.
create_container_if_not_exists = True
