[default]
providers = write-local, memory-cache
strategy = all

# By default, use zlib compression with level 1
[compression]
algorithm = zlib
level = 1

[write-local]
provider = filesystem
path = /tmp/cache

[memory-cache]
provider        = in-memory
persist-key     = standard
exists          = true
